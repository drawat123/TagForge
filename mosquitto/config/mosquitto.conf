listener 1883

# No authentication yet. Device identity comes from MQTT credentials later, which
# is a decision that belongs with the identity model rather than with transport.
allow_anonymous true

persistence false
log_dest stdout
